<?php

declare(strict_types=1);

// Root autoload.files entry (present in the dumped autoload_files.php): loaded
// eagerly, so a require targeting it is redundant.
define('VENDOR_AUTOLOAD_PROJECT_BOOTSTRAPPED', true);
