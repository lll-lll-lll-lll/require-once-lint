<?php

declare(strict_types=1);

// Not referenced by composer.json (that would make depone treat any require
// to it as redundant) -- it exists purely so the dumped autoload_files.php
// below has a real file to point ComposerLoaderVerifier::verifyEagerTarget()
// at.
